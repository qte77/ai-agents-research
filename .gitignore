# OS generated files
.DS_Store
Thumbs.db

# Plugin-deployed files (source of truth: claude-code-plugins)
.claude/scripts/
.claude/skills/

# Local-only agent handoff notes (per-session scratch, not shared)
.claude/handoffs/

# Tooling caches (per-session artifacts, never tracked)
.gh-cache/

# Local-only auto-memory (per-clone working scratchpad, not shared)
/MEMORY.md

# bwrap sandbox phantom files
# Bug: $HOME-dotfile deny list resolved against process.cwd() leaks 0-byte
# char-special /dev/null bind-mount targets into the project root. See
# https://github.com/qte77/ai-agents-research/blob/main/docs/cc-native/sandboxing/CC-sandbox-bwrap-host-quirks.md
# Tracked: anthropics/claude-code#17727, sandbox-runtime#139
/.bash_profile
/.bashrc
/.claude/agents
/.claude/commands
/.claude/skills
/.env
/.gitconfig
/.gitmodules
/.idea
/.mcp.json
/.profile
/.ripgreprc
/.vscode
/.zprofile
/.zshrc
# Git internals leaked by denyWithinAllow auto-generation dropping the
# .git/ prefix — same root cause class
/HEAD
/config
/hooks
/objects
/refs

# Python artifacts
__pycache__/
*.pyc

# graphify knowledge graph — generated, regenerable; published to the gh-pages
# branch (https://qte77.github.io/ai-agents-research/), never tracked on main.
graphify-out/

# lychee scheduled-monitor report (generated by `make check_links_report`)
lychee-report.md
